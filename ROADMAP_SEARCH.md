# ROADMAP: LocalWiki 검색 기능 개선

## 🏗 현재 상태 분석

### 현재 구현된 기능
- **백엔드** (`GET /api/search?q=`): 
  - 모든 `.md` 파일을 순차적으로 읽어 `String.includes()`로 단순 문자열 매칭
  - 일치하는 첫 번째 라인을 스니펫으로 반환
  - 검색 결과 정렬/랭킹 없음, 인덱스 없음
- **프론트엔드** (`searchContent()`):
  - 300ms 디바운스 적용 (입력 0.3초 후 검색 실행)
  - 검색 결과를 메인 영역에 리스트 형태로 출력
  - 결과 개수, 제목, 스니펫, 파일 경로 표시
  - 결과 클릭 시 해당 페이지로 이동

### 한계점
| 문제 | 설명 |
| :--- | :--- |
| **성능** | 검색할 때마다 모든 `.md` 파일을 디스크에서 읽음. 파일이 많아질수록 속도 저하 |
| **정확도** | 단순 `includes()` 매칭. '요리' 검색 시 '요리사', '요리법' 모두 매칭되나 랭킹 없음 |
| **UX** | 검색 결과 내 하이라이트 없음. 스니펫이 단순 첫 매칭 라인 |
| **오타** | 오타에 대한 유연성 없음 (`레시피` ≠ `레시피`) |
| **멀티워드** | '소고기 무국' 검색 시 두 단어가 각각 포함된 문서를 찾지 못함 |

---

## 🎯 목표: 단계별 개선 로드맵

### Phase 1: 검색 인덱스 도입 (성능 개선) — `고` 우선순위

#### 문제
검색할 때마다 전체 파일 스캔. 파일이 50개 이상으로 늘어나면 응답 시간 500ms 이상 소요.

#### 해결 방안
- **메모리 내 인덱스 구축**: 서버 시작 시 `searchIndex`를 구축
- **증분 업데이트**: `chokidar` 이벤트(`add`, `change`, `unlink`) 발생 시 인덱스만 부분 업데이트
- **인덱스 구조**:

```javascript
{
  // 단어 → 파일 매핑
  terms: {
    '소고기': [{ path: 'recipe/...', count: 5, positions: [10, 45, ...] }],
    '무국': [{ path: 'recipe/...', count: 3, positions: [20, 80] }]
  },
  // 파일 메타데이터
  files: {
    'recipe/20260706_소고기_무국.md': {
      title: '소고기_무국',
      path: 'recipe/20260706_소고기_무국.md',
      content: '...',
      wordCount: 450
    }
  }
}
```

#### 구현 상세
```javascript
// searchIndex.js
const fs = require('fs');
const path = require('path');

class SearchIndex {
  constructor(dataDir) {
    this.dataDir = dataDir;
    this.terms = new Map();    // term → Set<filePath>
    this.files = new Map();    // filePath → meta
  }

  build() {
    // 최초 전체 인덱싱
  }

  updateFile(filePath) {
    // 단일 파일 증분 업데이트
  }

  removeFile(filePath) {
    // 파일 삭제 시 인덱스 정리
  }

  search(query) {
    // 토큰화 → 검색 → 랭킹 → 결과 반환
  }
}
```

#### 검증 기준
- 100개 파일 기준 검색 응답 시간 **100ms 이내**
- 파일 추가/수정 시 자동 색인 갱신

---

### Phase 2: 검색 결과 랭킹 및 정확도 개선 — `고` 우선순위

#### 문제
현재 검색 결과는 순서 없이 반환되며, 단순 `includes()`로 인해 부정확.

#### 해결 방안
- **멀티워드 검색**: "소고기 무국" → `소고기` AND `무국` 검색
- **랭킹 알고리즘 (TF 기반)**:
  | 조건 | 가중치 |
  | :--- | :--- |
  | 제목에 검색어 포함 | +10점 |
  | 제목이 검색어와 정확히 일치 | +20점 |
  | 본문에 검색어 1회 등장 | +1점 |
  | 본문에 검색어 5회 이상 등장 | +5점 (최대) |
  | 최근 수정된 파일 | 가중치 * 1.2 |
- **부분 일치 지원**: `무국` 검색 시 `소고기무국`도 검색 가능하도록 토큰화

#### 검증 기준
- '소고기' 검색 시 제목에 '소고기'가 포함된 파일이 본문에만 있는 파일보다 상위 노출

---

### Phase 3: 프론트엔드 검색 UX 개선 — `중` 우선순위

#### 문제
현재 검색 결과 UI가 단순하며, 페이지 내 검색어 하이라이트가 없음.

#### 해결 방안
- **검색어 하이라이트**: 스니펫 내 검색어를 `<mark>` 태그로 강조
  ```javascript
  function highlightSnippet(text, query) {
    const regex = new RegExp(`(${escapeRegex(query)})`, 'gi');
    return text.replace(regex, '<mark class="bg-yellow-200 px-0.5 rounded">$1</mark>');
  }
  ```
- **페이지 내 검색어 하이라이트**: 검색 결과에서 페이지 이동 시, 해당 페이지 로드 후 검색어 하이라이팅
  ```javascript
  // loadPageContent 후 .prose 내 검색어 마킹
  ```
- **결과 미리보기 확장**: 현재 1줄 스니펫 → 검색어 전후 문맥 2~3줄 표시
- **로딩 인디케이터**: 검색 중 스피너 표시
- **검색 결과 필터**: 카테고리(디렉토리)별 필터링

#### 검증 기준
- 검색 결과 스니펫에 검색어 하이라이트 적용
- 페이지 이동 후에도 검색어 하이라이트 유지

---

### Phase 4: 고급 검색 기능 — `중` 우선순위

#### 문제
오타에 취약하고 AND/OR 같은 고급 검색어를 지원하지 않음.

#### 해결 방안
- **오타 허용 (Fuzzy Search)**: Levenshtein distance 기반 유사어 검색
  ```javascript
  function fuzzyMatch(word, query, maxDistance = 2) {
    // Levenshtein distance 계산
  }
  ```
- **검색어 연산자 지원**:
  - `소고기 무국` → AND 검색 (기본)
  - `소고기 OR 돼지고기` → OR 검색
  - `"소고기 무국"` → 구문 검색(정확히 일치)
  - `-김치` → 제외 검색
- **태그 기반 검색**: 파일 상단에 `tags: [소고기, 국]` 메타데이터가 있을 경우 태그 검색 지원
- **검색 제안 (Autocomplete)**: 입력 중 실시간 검색어 추천

#### 검증 기준
- `유아식` 오타 → `유야식`으로 검색해도 결과 반환
- `소고기 -김치` 검색 시 김치 관련 문서 제외

---

### Phase 5: 검색 히스토리 및 개인화 — `하` 우선순위

#### 문제
자주 찾는 문서를 다시 검색해야 하는 불편함.

#### 해결 방안
- **최근 검색어 저장**: `localStorage`에 최근 10개 검색어 저장
- **자주 방문한 페이지**: 클릭 횟수 기반 인기 문서 추천
- **검색 통계 대시보드** (선택): 가장 많이 검색된 키워드 통계

---

## 📋 구현 일정

| Phase | 내용 | 예상 작업량 | 의존성 |
| :--- | :--- | :--- | :--- |
| **1** | 검색 인덱스 도입 | `index.js` + `searchIndex.js` 신규 파일 | 없음 |
| **2** | 랭킹 및 정확도 개선 | `searchIndex.js` 수정 | Phase 1 |
| **3** | 프론트엔드 UX 개선 | `app.js` + `index.html` 수정 | Phase 1, 2 |
| **4** | 고급 검색 기능 | `searchIndex.js` + `app.js` | Phase 3 |
| **5** | 검색 히스토리 | `app.js` (`localStorage`) | Phase 3 |

---

## ⚠️ 리스크 및 고려사항

1. **메모리 사용량**: 1000개 파일 기준 인덱스 크기는 약 10~20MB 예상. 큰 부담 없음.
2. **한국어 토큰화**: 한국어는 띄어쓰기 단위 토큰화가 까다로움. 조사/어미 처리를 위해 간단한 형태소 분석기 도입 검토.
3. **WebSocket 연동**: 파일이 변경될 때 실시간 인덱스 업데이트 필요 (`chokidar` + `socket.io`).
4. **보안**: 검색 API 인증 불필요 (로컬 전용 앱).

---

## ✅ 체크리스트

- [ ] Phase 1: `SearchIndex` 클래스 구현 및 `/api/search` 연동
- [ ] Phase 1: `chokidar` 이벤트와 인덱스 증분 업데이트 연동
- [ ] Phase 2: TF 기반 랭킹 알고리즘 구현
- [ ] Phase 2: 멀티워드 AND/OR 검색 지원
- [ ] Phase 3: 검색 스니펫 하이라이트 (프론트엔드)
- [ ] Phase 3: 페이지 내 검색어 하이라이트
- [ ] Phase 3: 검색 UX 개선 (로딩, 에러 처리, 모바일 대응)
- [ ] Phase 4: Fuzzy search (오타 허용)
- [ ] Phase 4: 검색 연산자 지원
- [ ] Phase 4: Autocomplete
- [ ] Phase 5: 검색 히스토리 (`localStorage`)
- [ ] Phase 5: 자주 방문한 페이지 추천